.globl _start

.data 

.text

_start:
	addi t0, zero, 3
	addi t1, zero, 12
	
	sub t0, t0, t1
	bge t0, zero, IF
	addi t1, t1, -1
	
IF: 	addi t0, zero, 0

exit:
    addi x17, x0, 10  # call number 10 = exit
    ecall
